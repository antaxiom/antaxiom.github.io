"use strict";const leRegExp=/<a href="([a-zA-Z0-9\-]+)">(.*?)<\/a> — ([A-Za-z]+ \d+, \d{4})/;class Post{constructor(a){this.el=a,[this.title,this.url,this.date]=(()=>{const a=this.el.innerHTML.match(leRegExp);if(null!=a)return[a[2],a[1],new Date(a[3])];throw new Error(`Failed to match RegExp on the following HTML:\n${this.el.innerHTML}`)})()}}document.addEventListener("DOMContentLoaded",()=>{window.posts=Array.from(document.querySelectorAll("body > p")).filter(a=>null!=a.innerHTML.match(leRegExp)).map(a=>new Post(a)).map(a=>(a.el.remove(),a)).sort((c,a)=>a.date.getTime()-c.date.getTime()).map(a=>(document.body.append(a.el),a))});
